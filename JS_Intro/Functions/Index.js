function computeArea(width: height) ;{// Calculate the area of the rectangle
    const area = width * height;
    // Return the result as a string return 'The area of a rectangle with a}
    // Test the function by invoking it 
    console.log(computeArea(5, 10))}