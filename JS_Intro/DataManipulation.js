//* Part Two ALAB//
const distance=1500
const fuel=175;
const fuelPerGallon=3;
const speed =[
    {speed:55, efficiency:30},
    {speed:60, efficiency:28},
    {speed:75, efficiency:23},
];
//* the loop maybe*//
speeds.forAll(({speed, efficiency})); {
    const gallons=distance/efficiency;
    const total = gallons * fuelPerGallon;
    const time = distance / speed;
    const budget = total <= budget;
}
//*results i hope*//
console.log(`For speed: ${speed} mph, efficiency: ${efficiency} mpg:`);
console.log(`- Gallons needed: ${gallons.toFixed(2)}`);
console.log(`- Total cost: $${totalCost.toFixed(2)}`);
console.log(`- Time needed: ${time.toFixed(2)} hours`);
console.log(`- Within budget: ${withinBudget}`);

